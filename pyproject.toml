[build-system]
# Should be mirrored in requirements.txt
requires = [
    "setuptools>=64",
    "setuptools-scm>=8",
    "decorator",
    "pyyaml",
    "scipy",
    "vllm==0.7.3",
]
build-backend = "setuptools.build_meta"
